#include <stdio.h>
#include <stdlib.h>
int main ()
{
    int age = 17;
    int wallet = 6000 ;
    if (age >=18 && wallet >= 3000) {
        printf ("\nnoi vao");
    }else{
        printf ("\ncon do ngheo khi");
    }
}